# Forms-Engine example config (FR6-2) — the only file you need to edit.
# Run `node ../seed/seed.mjs` (with the backend up: `docker compose up`) and
# paste the printed publicId below, then `npm run dev`.
VITE_FE_API_BASE=http://localhost:8080
VITE_FE_PUBLIC_ID=PASTE_PUBLIC_ID_HERE
